import { NgModule } from '@angular/core';
import { SharedModule } from 'src/app/shared/shared.module';

import { {{pascalCase componentName}}RoutingModule } from './{{componentName}}-routing.module';
import { {{pascalCase componentName}}Component } from './{{componentName}}.component';
{{#if (or buttons.hasAddButton buttons.hasEditButton)}}
import { {{pascalCase componentName}}DetailComponent } from './detail/{{componentName}}-detail.component';
{{/if}}


@NgModule({
    declarations: [
        {{pascalCase componentName}}Component,
      {{#if (or buttons.hasAddButton buttons.hasEditButton)}}
        {{pascalCase componentName}}DetailComponent
      {{/if}}
    ],
    imports: [
        SharedModule,
        {{pascalCase componentName}}RoutingModule,
    ]
})
export class {{pascalCase componentName}}Module { }
