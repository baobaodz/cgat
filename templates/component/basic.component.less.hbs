@import "~src/styles/variables";

.{{selectorPrefix}}-{{componentName}}-container {
  padding: 24px;
  background: @white;
  border-radius: @border-radius-base;

  .ant-table {
    margin-top: 16px;
  }

  .action-bar {
    margin-bottom: 16px;
    display: flex;
    justify-content: space-between;
  }
}
